'use client';

interface ApprovalMetricsTabProps {
  data: any;
}

/**
 * Approval Metrics tab — placeholder for Commit 4.
 */
export function ApprovalMetricsTab({ data }: ApprovalMetricsTabProps) {
  return (
    <div className="text-center py-12 text-surface-500">
      <p className="text-sm">Approval Metrics tab — coming soon.</p>
    </div>
  );
}
